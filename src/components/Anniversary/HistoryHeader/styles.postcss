
.l-history-header {
	.wrap {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding-top: rem(10);
		padding-bottom: rem(10);
	}

	.history-logo {
		height: rem(40);
	}

	.aux {
		visibility: hidden;
	}
}